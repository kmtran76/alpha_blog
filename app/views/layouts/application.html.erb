<!DOCTYPE html>
<html>
  <head>
    <title>AlphaBlog</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <!--class="d-flex flex-column" is for sticky footer -->
  <body class="d-flex flex-column">
    
    <%= render 'layouts/navigation'%>
    <%# 
    id="page-content" is for sticky footer 
    (https://startbootstrap.com/snippets/sticky-footer-flexbox)
    %>
    <div id="page-content">
      <!-- Notice we dont reference the messages partial (_messages.html.erb) with an _ -->
      <%= render 'layouts/messages'%>
      <%= yield %>
    </div>
    <%= render 'layouts/footer'%>
  </body>
</html>
